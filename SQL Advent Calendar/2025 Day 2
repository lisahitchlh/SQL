-- Santa wants to analyze which toys that were produced in his workshop have already been delivered to children. 
-- You are given two tables on toy production and toy delivery
-- can you return the toy ID and toy name of the toys that have been delivered?

SELECT 
  tp.toy_id,
  tp.toy_name
FROM toy_production as tp
JOIN toy_delivery as td
ON tp.toy_id = td.toy_id