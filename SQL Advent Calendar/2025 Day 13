-- Santa's audit team is reviewing this year's behavior scores to find the extremes
-- write a query to return the lowest and highest scores recorded on the Naughty or Nice list.


WITH max_scores AS
(SELECT MAX(behavior_score) AS max_score
  , MIN(behavior_score) AS min_score
  FROM behavior_scores)

  SELECT behavior_score
  FROM behavior_scores
  WHERE behavior_score = (SELECT max_score FROM max_scores) 
    OR behavior_score = (SELECT min_score FROM max_scores)